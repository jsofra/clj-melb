;; Setup cljs-template 
'[$ j play]
'[$ lein new cljs-template lein-demo]
'[$ cd lein-demo]
'[$ lein run]
'[$ lein cljsbuild repl-listen]
'[$ e .]
;; Browse to localhost:8090

;; Show everyone the directory structure

;; Mess around with the code